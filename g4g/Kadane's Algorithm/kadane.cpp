#include <iostream>
#include <cmath>
#include <algorithm>

using namespace std;

int main(void)
{
    int t;
    cin >> t;
    while(t--)
    {
        cout << t;
        int n, pre = INFINITY, cur;
        cin >> n;
        int a[n];
        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
            pre = max(a[i], )
        }

    }
    return 0;
}
