#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    map<int, int> m;
    m[0] = 1;
    m[10] = 1;
    cout << m[2];
    for(auto u : m) cout << *u << m[*u] << "\n";
    // int n, x; cin >> n >> x;
    // int a[n];
    // for(int i = 0; i < n; i++) cin >> a[i];

}
