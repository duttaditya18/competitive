#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    int l, n; cin >> l >> n;
    for(int i = 1; i <= n; i++) cin >> a[i];

    int dp[n + 1][l + 1][l + 1] = {};

    for(int i = 1; i <= n; i++)
    {

    }
}
